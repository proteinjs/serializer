{
  "name": "root",
  "private": true,
  "scripts": {
    "clean-workspace": "clean-workspace",
    "build-workspace": "build-workspace",
    "test-workspace": "test-workspace",
    "watch-workspace": "watch-workspace"
  },
  "devDependencies": {
    "@proteinjs/build": "^1.0.10",
    "lerna": "8.1.2"
  }
}
